---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import ArticleCard from '../components/ArticleCard.astro';

const allArticles = await getCollection('articles', ({ data }) => !data.draft && data.type === 'comparatif');
allArticles.sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());
const readingTime = (body: string) => Math.ceil(body.split(/\s+/).length / 200);
---

<Layout
  title="Comparatifs outils social media — ViralKit"
  description="Comparatifs détaillés entre les meilleurs outils pour Instagram, TikTok et YouTube. Tableaux de fonctionnalités, prix et recommandations."
>
  <main class="mx-auto max-w-5xl px-4 py-12">
    <div class="mb-10">
      <span class="inline-block text-xs font-semibold text-pink-600 bg-pink-50 px-2 py-0.5 rounded-md uppercase tracking-wide mb-3">Comparatifs</span>
      <h1 class="text-3xl font-bold text-slate-900 tracking-tight">Comparatifs outils social media</h1>
      <p class="mt-3 text-lg text-slate-600">Les comparaisons côte-à-côte pour choisir le bon outil sans se tromper.</p>
    </div>
    {allArticles.length > 0 ? (
      <div class="grid gap-5 md:grid-cols-2 lg:grid-cols-3">
        {allArticles.map((article) => (
          <ArticleCard
            title={article.data.title}
            description={article.data.description}
            slug={article.slug}
            category={article.data.category}
            type={article.data.type}
            date={article.data.date}
            readingTime={readingTime(article.body)}
          />
        ))}
      </div>
    ) : (
      <p class="text-slate-500">Aucun comparatif publié pour l'instant.</p>
    )}
  </main>
</Layout>
